// Static drawn silhouette of the player being built. (It no longer changes with
// the chosen body stats — just a fixed figure.)
export function PlayerSilhouette() {
  return <img className="silhouette" src="/silhouette.png" alt="Your player" />;
}
